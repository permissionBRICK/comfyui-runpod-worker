# comfyui-runpod-worker
Custom `runpod/worker-comfyui` image with ComfyUI-GGUF + comfyui-tooling-nodes baked in.
Pulled by the RunPod serverless endpoint that serves as the cloud tier of the
SillyTavern image-generation fallback chain. Models live on the RunPod network volume.
